<div class="site-mobile-nav" id="site-mobile-nav" data-mobile-nav tabindex="0">
  <div class="mobile-nav-panel" data-mobile-nav-panel>

    {% render 'site-header-actions' %}

    <a
      class="mobile-nav-close"
      href="#site-header-nav"
      data-mobile-nav-close>
      {% render 'icon-menu-close' %}
      <span class="visually-hidden">{{ 'general.accessibility.close' | t }}</span>
    </a>

    <div class="mobile-nav-content" data-mobile-nav-content>
      {% assign cl_editable_menu = section.settings.cl_editable_mega_menu %}
      {% if section.settings.cl_use_editable_mega_menu and cl_editable_menu != blank and cl_editable_menu.links != blank %}
        {% render 'cl-native-mega-menu-editable', mode: 'mobile', menu: section.settings.cl_editable_mega_menu, show_images: section.settings.cl_mega_menu_show_images %}
      {% else %}
        {% render 'cl-native-mega-menu', mode: 'mobile' %}
      {% endif %}

      {% if section.settings.secondary_menu != blank %}
        <span class="mobile-nav-content__border"></span>
        <div class="mobile-nav-content__secondary-menu">
          {%
            render 'navlist',
            menu_list: linklists[section.settings.secondary_menu].links,
            depth: 1,
            max_depth: 1,
            aria_expanded: true,
          %}
        </div>
      {% endif %}
    </div>
    <div class="utility-bar__mobile-disclosure" data-utility-mobile></div>
  </div>

  <div class="mobile-nav-overlay" data-mobile-nav-overlay></div>
</div>
